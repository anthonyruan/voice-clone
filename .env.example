# Fish Audio API Configuration
# Required: Your Fish Audio API key for voice cloning and TTS services
FISH_AUDIO_API_KEY=your_fish_audio_api_key_here

# CORS Configuration
# Optional: Comma-separated list of allowed origins for CORS requests
# Default: http://localhost:3000
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Environment Configuration
# Optional: Environment mode (development, production, test)
# Default: development
NODE_ENV=development

# File Upload Configuration
# Optional: Maximum file size for uploads in MB (1-50)
# Default: 10
MAX_FILE_SIZE_MB=10

# Database Configuration (if needed for production)
# DATABASE_URL=sqlite:./voice_clone.db

# Security Configuration (optional)
# Set to true to enable additional security measures in production
# ENABLE_SECURITY_HEADERS=true

# Logging Configuration (optional)
# LOG_LEVEL=info

# Rate Limiting Configuration (optional)
# RATE_LIMIT_MAX_REQUESTS=10
# RATE_LIMIT_WINDOW_MS=60000